export interface Exercise {
    numero: number,
    title: string,
    disable: boolean,
    routerLink?: any,
}

export interface Country {
    name: string
}

export interface Light {
    color: string
  }